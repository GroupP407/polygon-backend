name: 'test my project'
on:
  push:

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - uses: actions/setup-dotnet@v1
        name: 'setup dotnet'
        with:
          dotnet-version: '5.0.x'
          
      - name: 'install'
        run: dotnet buid Polygon